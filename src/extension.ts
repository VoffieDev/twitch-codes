import { type ExtensionContext } from "vscode";

export function activate(_context: ExtensionContext) {
  console.log('Congratulations, your extension "twitch-codes" is now active!');
}

export function deactivate() {}
